/**
 * Picocli command-line entry points: {@code learn} and {@code setup}, plus the
 * virtual-env helper used to locate the bundled SkillSpector install.
 */
@NullMarked
package se.deversity.skill3.cli;

import org.jspecify.annotations.NullMarked;
