/**
 * Tiny console-output helpers (e.g. {@link se.deversity.skill3.console.ProgressBar}).
 *
 * <p>A neutral leaf layer: it depends on nothing else in the application, so any layer —
 * including the composition root and the domain pipeline — may use it to report progress
 * without creating a dependency cycle.
 */
package se.deversity.skill3.console;
