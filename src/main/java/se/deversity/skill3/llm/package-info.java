/**
 * Local-LLM synthesis: the OpenAI-compatible chat client, the synthesizer that turns a
 * scored context bundle into a draft, and the deterministic {@code SKILL.md} post-processor
 * that guarantees spec-compliant frontmatter regardless of model output.
 */
@NullMarked
package se.deversity.skill3.llm;

import org.jspecify.annotations.NullMarked;
