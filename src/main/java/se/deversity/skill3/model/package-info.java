/**
 * Immutable value types shared across the pipeline: the resolved knowledge {@code Cutoff},
 * a discovered {@code Source}, and the assembled {@code ContextBundle}.
 */
@NullMarked
package se.deversity.skill3.model;

import org.jspecify.annotations.NullMarked;
