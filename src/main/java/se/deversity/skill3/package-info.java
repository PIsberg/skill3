/**
 * Skill3 — a fully local AI Skill relearner. This root package holds the CLI entry
 * point ({@link se.deversity.skill3.Skill3App}) and the orchestration pipeline
 * ({@link se.deversity.skill3.LearnPipeline}).
 *
 * <p>All types in this package and its sub-packages are {@link org.jspecify.annotations.NullMarked}:
 * every parameter, field, and return type is non-null by default; nullable elements are
 * annotated {@code @Nullable} explicitly.
 */
@NullMarked
package se.deversity.skill3;

import org.jspecify.annotations.NullMarked;
